name: to_do_list_mob_x
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  mobx: ^2.5.0
  flutter_mobx: ^2.3.0
  uuid: ^4.5.1
  drift: ^2.28.1
  drift_flutter: ^0.2.5
  path_provider: ^2.1.5
  flutter_modular: ^6.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  mobx_codegen: ^2.7.2
  drift_dev: ^2.28.2
  build_runner: ^2.7.1

flutter:

  uses-material-design: true
